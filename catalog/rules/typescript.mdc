---
description: TypeScript conventions
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

Prefer explicit types on exported functions. Do not use `any` without a comment. Handle rejected promises. Colocate tests with the project’s existing runner (Vitest, Jest, etc.). Run the repo’s `typecheck` / `lint` scripts when present.
